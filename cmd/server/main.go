// Package main is the AgentKMS production server entrypoint.
// Implementation tracked in backlog under stream A (auth), C (API handlers),
// and D (local dev).  This stub exists so the project structure compiles.
package main

func main() {}
